import type { Metadata } from "next";
import SitePage from "../SitePage";
export const metadata: Metadata = { title: "Selected Work", description: "Selected website, e-commerce, and custom web solution concepts from Launchpad Web Solutions." };
export default function Page() { return <SitePage page="portfolio"/>; }
